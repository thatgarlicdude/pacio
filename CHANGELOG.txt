2026-01-09 GarlicDude <https://www.github.com/thatgarlicdude>

	PacIO Version 1.6.0-alpha
	* Added the docs directory.
	* Updated build.gradle.
		* Added the javadoc extension.
		* The javadoc task will output the contents in the newly added docs
		directory.
	* Updated package-info.java from the pacio package.
		* The Javadoc now uses paragraphs.
		* Redescribed the Javadoc comment for the package, now calling it
		the main package of the library.
	* Updated PacIO.java.
		* The Javadoc now uses paragraphs.
		* Compacted the code.
	* Updated package-info.java from the file package.
		* The Javadoc now uses paragraphs.
		* Reworked on the Javadoc comment, now explaining what it's used
		for.
	* Updated PacObject.java.
		* Removed the name variable, as it is already stored in the path
		variable.
		* The method getName() now functions differently, now returning
		with path.getFileName.toString().
	* Updated PacFile.java.
		* The Javadoc now uses paragraphs.
	* Updated PacDirectory.java.
		* The Javadoc now uses paragraphs.
	* Updated Loadable.java.
		* The Javadoc now uses paragraphs.
	* Updated Savable.java.
		* The Javadoc now uses paragraphs.

2026-01-01 GarlicDude <https://www.github.com/thatgarlicdude>

	PacIO Version 1.5.1-alpha
	* Renewed the copyright year for each Java file to 2026.
	* Updated the package-info.java for
	io.github.thatgarlicdude.pacio.file.
		* Changed the comment describing the package to represent it
		better.

2025-12-29 GarlicDude <https://www.github.com/thatgarlicdude>

	PacIO Version 1.5.0-alpha
	* Updated PacRomSet.java.
		* Renamed it to PacDirectory.java.
		* The roms variable is now called files, and is a final List
		instead of an ArrayList.
		* The method getFiles now returns an unmodifiable list instead
		of pointing to the list.
		* Renamed the open methods to from.
		* Removed the openRoms method.
		* Lots of cleanup.
	* Added PacObject.java.
		* This serves as a replacement for PacFile.java.
	* Added Loadable.java.
		* Loadable features two abstract methods: load and unload.
	* Updated PacRom.java.
		* Renamed it to PacFile.java, not to be confused with the one
		that's deleted.
		* Removed the DEFAULT_DATA variable.
		* Renamed the open methods to from.
		* The readN method has been renamed to read.
		* The writeN method has been renamed to write.
		* Lots of cleanup.
	* Removed the io.github.thatgarlicdude.pacio.roms package in the test
	src.
	* Added TestPacIO.java.
		* This is a test class used for testing out the methods in the
		PacIO class.

2025-12-18 GarlicDude <https://www.github.com/thatgarlicdude>

	PacIO Version 1.4.0-alpha
	* Removed PacRomOpener.java.
		* The static open methods were transferred to PacRom.java
		instead.
	* Removed PacRomSetOpener.java.
		* Like PacRomOpener, the static open methods were moved to
		PacRomSet.java.
	* Added Savable.java.
		* It's an interface used for saving a file in memory.
	* Updated Closable.java.
		* The class is now public.
		* The class also now has a description.
		* The file now has a copyright message.
	* Updated PacRom.java.
		* The class now implements the newly added Savable interface.
	* Updated PacRomSet.java.
		* Like PacRom.java, it implements the Savable interface.
		* save() saves the ROM files inside the directory.

2025-12-14 GarlicDude <https://www.github.com/thatgarlicdude>

	PacIO Version 1.3.0-alpha
	* Added PacIO.java.
		* That is the main class for the PacIO library.
		* It contains functions for creating PacRoms and PacRomSets.
	* Added the rom package to the pacio package.
		* All java files were moved to this directory.
		* The rom package is also in the src/test/java directory.

2025-12-13 GarlicDude <https://www.github.com/thatgarlicdude>

	PacIO Version 1.2.0-alpha
	* Updated RomOpener.java.
		* RomOpener.java has been renamed to PacRomOpener.java.
		* This object is now an interface instead of a class.
		* Removed methods that relate to the PacRomSet class.
	* Added PacRomSetOpener.java.
		* Code from PacRomOpener.java were transferred there.
	* Updated ResourceGetter.java.
		* Changed rom1 to be a path to the first ROM file of the test
		ROM set.
		* Because of this, getTestRom1() and getTestRom1URI() were added.
	* Added TestPacRomOpener.java.
		* This test class is used for testing the three open methods.
	* Added TestPacRomSetOpener.java.
		* The same thing as TestPacRomOpener.java, but for TestRomSet
		objects.

2025-12-12 GarlicDude <https://www.github.com/thatgarlicdude>

	PacIO Version 1.1.0-alpha
	* Added RomOpener.java.
		* This class contains helper methods for opening PacRoms and
		PacRomSets.
		* Those helper methods can use Path, URI, or String objects as
		the arguments.
		* It can also help create an ArrayList of PacRoms for the
		PacRomSet.
	* Removed Openable.java.
	* Added Closable.java as a replacement for Openable.java.
		* It only contains an abstract close method.
	* Updated PacFile.java.
		* Removed the other constructors, only leaving with one.
	* Updated PacRom.java.
		* The main constructor is now set to default instead of public
		to make up for the RomOpener.
		* Like PacFile.java, the other constructors were removed.
		* PacRom no longer implements Openable.
		* Instead, PacRom implements the newly added Closable interface.
		* The close method now clears its data array with zeroes.
	* Updated PacRomSet.java.
		* Changes from PacRom.java were applied as well.
		* For the close method, it now closes the PacRoms in the PacRomSet.
		* The roms variable is now private.
		* To make up for that, getRoms() was added.
	* Updated ResourceGetter.java.
		* ResourceGetter now has two methods: One for getting the URI of
		the test ROM set, and the other being the PacRom of it.
	* Updated TestPacRom.java.
		* The test methods are now shortened due to the new helper methods.
	* Updated TestPacRomSet.java.
		* Changes from TestPacRom.java were also applied as well.
		* testGetPath() now prints the path.

2025-12-11 GarlicDude <https://www.github.com/thatgarlicdude>

	PacIO Version 1.0.1-alpha
	* Updated all the Java files.
		* Every Java file now have tabbed gaps in between each member of
		a class.
	* Updated PacFile.java.
		* Added another main constructor, which sets a custom name to the
		object.
	* Updated PacRom.java.
		* Added a main constructor, which sets the path and custom name to
		the object.
		* The path constructor now calls open() after the super
		constructor.
		* Because of this, all the other constructors throw the
		IOException.
		* The other constructors now use this() instead of super().
	* Updated PacRomSet.java.
		* Same thing as PacRom.java.
	* Updated TestPacRom.java.
		* Because PacRomSets can be automatically opened, there's no need
		to call open() from the testRomSet.
	* Updated TestPacRomSet.java.
		* Same thing as TestPacRom.java.

2025-12-05 GarlicDude <https://www.github.com/thatgarlicdude>

	PacIO Version 1.0.0-alpha
	* Added the base files.
	* Added PacFile, the base class for PacRom and PacRomSet.
		* Has the path and name variables, as well as their getter methods.
	* Added PacRom, a class that represents a ROM file within a ROM
	set directory.
		* PacRoms contain byte data from their repsective ROM file.
		* They can read and write data in the byte array.
	* Added PacRomSet, a class that represents a ROM set directory.
		* PacRomSets have an ArrayList containing PacRom objects.
		* PacRomSets can find a specific PacRom when using find().
